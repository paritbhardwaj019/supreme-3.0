#include <iostream> // import content of iostream header file, contain declarations of cin and cout
using namespace std; // by this line you tell compiler to use std symbols or components defined in c++ instead of std::cout

int main() 
{          

    cout << "Namaste Duniya :) \n"; // we can use \n for new line instead of endl;

    return -1; // all behave same either it would be 0,1,12,-1,-2. using 0 is a good way to represent that this code is successfully executed!
}